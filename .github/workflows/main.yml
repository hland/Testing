name: Test
on:
 pull_request:
 push:
        
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: |
          echo "nanananana"
  
  test-workflow-call:
     uses: hland/reusable-workflows/.github/workflows/reuse.yml@main
